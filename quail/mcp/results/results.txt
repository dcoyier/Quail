success_printed_output(printed_output):
  Give back a success CallToolResult with {"printed_output": printed_output}.

success_result(payload, text default None, images default empty):
  Give back a CallToolResult with is_error false and the payload as structured_content.
  When text is set, use it for the text content block.
  When text is omitted and images is empty, JSON-dump payload into a text block.
  When text is omitted and images is non-empty, omit the text block (image-only).
  Never merge text into structured_content.
  Append one ImageContent block per ToolImage after any text block (base64 data).

validate_time_window(time_window):
  Delegate to quail.analysis.limits.validate_time_window.
  Treat None as standard.
  Require standard or extended.

error_result(error, execution_id default None, repair_hint default None):
  Build a diagnostic from the exception.
  Give back a CallToolResult with is_error true, execution_id, and diagnostic.

diagnostic_from_exception(error, repair_hint default None):
  Classify the exception into error_class and stable_error_code.
  Remember message from the exception text.
  Prefer an explicit repair_hint; otherwise use QuailRuntimeError.repair_hint or
  ConnectorError.repair_hint when present.
  Give back the diagnostic object.

classify_exception(error):
  Map ConnectorError to ConnectorError / stable_code (lowercased).
  Map Quail, Auth, Session, Dataset types to stable codes.
  SessionSyntaxError becomes QuailScopeError / quail_scope_error (api.md table).
  ValueError becomes QuailSyntaxError / quail_syntax_error (tool arg shape).
  QuailServerBusyError becomes QuailRuntimeError / server_busy.
  QuailSessionBusyError becomes QuailRuntimeError / session_busy.
  Unknown errors become InternalError / internal_error.
