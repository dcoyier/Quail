Resolve and read the model-facing analysis contract.

There is a private repo fallback path at docs/api.md relative to the package root.
There is a packaged resource at quail/data/api.md (force-included from docs/api.md).

load_api_docs(override default None):
  If override is set, read that path and give back the text.
  Else if the packaged quail/data/api.md resource exists, give back its text.
  Else if the repo docs/api.md exists, give back its text.
  Else raise FileNotFoundError.
