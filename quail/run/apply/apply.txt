apply_config(config):
  For each declared dataset, require the source CSV file exists.
  Open the core database.
  Ensure every declared workspace exists.
  Import each CSV into its workspace with activate true (idempotent on same content).
  On failure, close the DB and raise.
  Give back the open CoreDb.
