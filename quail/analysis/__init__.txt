This package re-exports the public analysis surface.

Import and expose:
  errors, Field, Unit, entries, fields, Entry, make_entry
  Operation factories and Operation
  Expression, Predicate, GroupExpr, G0, G1, Ranking, ReFacade
  namespace callables retrieve, count, create_field, tag, untag, quail_print
  api_namespace

__all__ lists those public names for star-import and documentation.
