acquire_session_lock(session_id):
  Non-blocking in-process lock keyed by session_id.
  Raise QuailSessionBusyError when another call already holds the key.
  Always release on exit.

reset_session_locks_for_tests():
  Clear the held set for tests.
